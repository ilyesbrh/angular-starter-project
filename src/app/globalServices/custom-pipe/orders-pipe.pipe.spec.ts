import { OrdersPipePipe } from './orders-pipe.pipe';

describe('OrdersPipePipe', () => {
  it('create an instance', () => {
    const pipe = new OrdersPipePipe();
    expect(pipe).toBeTruthy();
  });
});
