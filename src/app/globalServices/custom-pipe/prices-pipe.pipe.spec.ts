import { PricesPipePipe } from './prices-pipe.pipe';

describe('PricesPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PricesPipePipe();
    expect(pipe).toBeTruthy();
  });
});
